# Commands to start on workspace startup
tasks:
  - name: Start Boy
    init: npm install
    command: npm run start

# Ports to expose on workspace startup
ports:
  - port: 3000
    onOpen: open-preview
    name: Simplistic Chat Bot Ever
    description: Simplistic Chat Bot Ever Website Preview